@model WAMVC.Models.DetallePedidoModel

@{
    ViewData["Title"] = "Crear detalle";
}

<div class="row mb-4">
    <div class="col">
        <h1>
            <i class="bi bi-plus-circle me-2"></i>Crear detalle
        </h1>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h4 class="mb-0">Nuevo detalle de pedido</h4>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-6">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    
                    <div class="form-group mb-3">
                        <label asp-for="IdPedido" class="control-label"></label>
                        <select asp-for="IdPedido" class="form-control" asp-items="ViewBag.IdPedido">
                            <option value="">-- Seleccione un Pedido --</option>
                        </select>
                        <span asp-validation-for="IdPedido" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group mb-3">
                        <label asp-for="IdProducto" class="control-label"></label>
                        <select asp-for="IdProducto" class="form-control" asp-items="ViewBag.IdProducto">
                            <option value="">-- Seleccione un Producto --</option>
                        </select>
                        <span asp-validation-for="IdProducto" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group mb-3">
                        <label asp-for="Cantidad" class="control-label"></label>
                        <input asp-for="Cantidad" class="form-control" type="number" min="1" value="1" />
                        <span asp-validation-for="Cantidad" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group mb-4">
                        <label asp-for="PrecioUnitario" class="control-label"></label>
                        <div class="input-group">
                            <span class="input-group-text">$</span>
                            <input asp-for="PrecioUnitario" class="form-control" type="number" step="0.01" min="0" />
                        </div>
                        <span asp-validation-for="PrecioUnitario" class="text-danger"></span>
                        <small class="text-muted">Usa punto (.) como separador decimal. Ejemplo: 10.50</small>
                    </div>
                    
                    <div class="form-group d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-plus-lg me-1"></i> Crear
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left me-1"></i> Volver a la lista
                        </a>
                    </div>
                </form>
            </div>
            <div class="col-md-6">
                <div class="alert alert-info">
                    <h5 class="alert-heading"><i class="bi bi-info-circle me-2"></i>Información</h5>
                    <p>El precio unitario se asignará automáticamente según el producto seleccionado.</p>
                    <hr>
                    <p class="mb-0">No olvide actualizar el monto total del pedido después de agregar productos.</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
